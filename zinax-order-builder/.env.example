# Copy this file to .env.local (gitignored) and fill in your own Supabase
# project's values — Project Settings → API in the Supabase dashboard.
# Leaving these unset is fine: the app falls back to local-only storage.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
